<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aconst.money4life">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RatesActivity" />
        <activity
            android:name=".ActionsActivity"
            android:label="@string/title_activity_actions"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ActionNewEditActivity" />
        <activity android:name=".CurrencyActivity" />
        <activity android:name=".RatesDownloadActivity" />
        <activity android:name=".TariffActivity" />
        <activity android:name=".TariffAddEditActivity" />
        <activity android:name=".CountersActivity" />
        <activity android:name=".AccountsActivity" />
        <activity android:name=".AccountAddEditActivity" />
        <activity android:name=".OperationsActivity" />
        <activity android:name=".OperationsAddEditActivity"></activity>
    </application>

</manifest>